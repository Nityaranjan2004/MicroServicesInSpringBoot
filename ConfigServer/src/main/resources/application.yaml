spring:
  application:
    name: ConfigServer

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Nityaranjan2004/microservice-cofiguration
          clone-on-start: true

server:
  port: 8085